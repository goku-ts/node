export const config = {
    local: {
        username: "postgres",
        password: "admin",
        database: 'school',
        host: "localhost",
        port: 5432,
        dialect: 'postgres',
    }
}